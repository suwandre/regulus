//! regulus hardware abstraction layer — core types, capability traits, and the
//! capability model shared across the engine and every hardware backend.

pub mod mock;
pub mod traits;
pub mod types;
